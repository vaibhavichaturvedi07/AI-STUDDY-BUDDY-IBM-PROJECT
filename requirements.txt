## ── Core web framework ────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # multipart/form-data (file upload)
aiofiles==23.2.1

## ── Database ─────────────────────────────────────────────────────────────
sqlalchemy>=2.0.44,<2.1          # includes Python 3.14 compatibility fixes
asyncpg==0.29.0                  # async PostgreSQL driver (runtime)
psycopg2-binary==2.9.9           # sync driver required by Alembic autogenerate
alembic==1.13.1                  # schema migrations

## ── Data validation ──────────────────────────────────────────────────────
pydantic==2.7.2
pydantic[email]==2.7.2           # EmailStr
pydantic-settings==2.3.1
email-validator==2.1.1           # required by pydantic[email]

## ── Authentication ───────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT encode / decode
bcrypt==4.1.3                     # password hashing
passlib[bcrypt]==1.7.4            # bcrypt wrapper used in tests

## ── Rate limiting ────────────────────────────────────────────────────────
slowapi==0.1.9                    # Starlette-native rate limiter

## ── Security headers ─────────────────────────────────────────────────────
secure==0.3.0                     # Secure-Headers middleware

## ── Document parsing ─────────────────────────────────────────────────────
pypdf==4.2.0                      # PDF text extraction
python-docx==1.1.0                # DOCX parsing

## ── AI providers (install the one you use) ───────────────────────────────
openai==1.30.5
## ibm-watsonx-ai==1.0.10        # uncomment for IBM watsonx.ai

## ── Dev / testing ────────────────────────────────────────────────────────
httpx==0.27.0                    # async HTTP client (also used by TestClient)
pytest==8.2.2
pytest-asyncio==0.23.7
aiosqlite==0.20.0                # async SQLite driver used by the isolated test database
pytest-cov==5.0.0                # coverage reports
anyio[trio]==4.4.0               # asyncio test backends
factory-boy==3.3.0               # test fixtures / factories
faker==25.2.0                    # realistic test data
